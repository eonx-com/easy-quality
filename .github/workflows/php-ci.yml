name: PHP-CI
on:
    push:

defaults:
    run:
        working-directory: ./

jobs:
    composer:
        runs-on: ubuntu-latest
        timeout-minutes: 60
        strategy:
            matrix:
                php: ['7.4']
        steps:
            -   uses: eonx-com/actions-checkout@v2

            -   uses: eonx-com/actions-setup-php@v2
                with:
                    php-version: ${{ matrix.php }}
                    coverage: none

            -   run: composer --version

            -   name: Get the cached Composer dependencies
                id: cache-composer
                uses: eonx-com/actions-cache@v1
                with:
                    path: vendor
                    key: ${{ matrix.php }}-composer-${{ hashFiles('composer.lock') }}

            -   name: Install dependencies
                if: steps.cache-composer.outputs.cache-hit == false
                run: |
                    composer install --prefer-dist --no-progress --no-scripts
    rector-tests:
        needs: composer
        runs-on: ubuntu-latest
        timeout-minutes: 60
        strategy:
            matrix:
                php: ['7.4']
        steps:
            -   uses: eonx-com/actions-checkout@v2

            -   uses: eonx-com/actions-setup-php@v2
                with:
                    php-version: ${{ matrix.php }}
                    coverage: none

            -   name: Get the cached Composer dependencies
                uses: eonx-com/actions-cache@v1
                with:
                    path: vendor
                    key: ${{ matrix.php }}-composer-${{ hashFiles('composer.lock') }}

            -   name: Run Rector tests
                run: |
                    vendor/bin/phpunit tests/Rector
    sniff-tests:
        needs: composer
        runs-on: ubuntu-latest
        timeout-minutes: 60
        strategy:
            matrix:
                php: ['7.4']
        steps:
            -   uses: eonx-com/actions-checkout@v2

            -   uses: eonx-com/actions-setup-php@v2
                with:
                    php-version: ${{ matrix.php }}
                    coverage: none

            -   name: Get the cached Composer dependencies
                uses: eonx-com/actions-cache@v1
                with:
                    path: vendor
                    key: ${{ matrix.php }}-composer-${{ hashFiles('composer.lock') }}

            -   name: Run Sniff tests
                run: |
                    vendor/bin/phpunit tests/Sniffs
